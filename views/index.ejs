<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/bootstrap.min.css'>
    <script src='/javascripts/react.min.js' type="text/javascript"></script>
    <script src='/javascripts/react-dom.js' type="text/javascript"></script>
    <!--<script src='/javascripts/react-with-addons.min.js' type="text/javascript"></script>-->
    <script src='/javascripts/browser.min.js' type="text/javascript"></script>
    <script src='/javascripts/jquery-1.8.1.js' type="text/javascript"></script>

  </head>
  <body>
    <div id="root" class="container col-md-6 col-md-offset-3"></div>
  </body>
  <script src="/javascripts/bundle.js" type="text/javascript"></script>
<script type="text/babel">
   /* var todoBox = React.createClass({
        getInitialState:function () {
            return{
                data:[
                    {"id": "0001", "task":"吃饭", "complete": "false"},
                    {"id": "0002", "task":"睡觉", "complete": "false"},
                    {"id": "0003", "task":"打豆豆", "complete": "true"},
                ]
            }
        }
    });
    var todoList = React.createClass({
        render:function () {
            var taskList = this.props.data.map(function (listItem) {
                return(
                    <todoItem
                            taskId={listItem.id}
                            key={listItem.id}
                            task={listItem.task}
                            complete={listItem.complete}
                            />

                )
            })
        }
    },this)

    ReactDOM.render(
            <h1>Hello,world</h1>,
        document.getElementById('root')
    )*/
</script>
</html>
